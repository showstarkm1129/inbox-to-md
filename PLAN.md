# プロジェクト計画書：ローカルMD自動整理・LLM連携アプリ

> ⚠️ **このファイルは凍結された意思決定ログです。**
> - 実装仕様は `STEP.md` を参照してください。
> - AIへの実装指示にこのファイルを使用しないでください。
> - 内容の変更は「プロジェクトの根本方針を変える場合」のみ行います。

---

## 1. プロジェクトの目的

授業や業務中の雑多なメモを、ユーザーが「どこに分類するか」を意識せずに書きなぐるだけで、LLM（Gemini API）が内容を解析し、ユーザーが独自に定義した「整理ルール（ワークフロー）」に従って自動で分割・要約・整理するローカルアプリケーションを構築する。

整理済みのメモは、ルールごとに指定されたフォルダ（例：category, diary等）にMarkdownファイルとして蓄積され、アプリ内で閲覧できる。

---

## 2. 技術スタック

| 項目 | 採用技術 |
|---|---|
| OS | Windows |
| バックエンド | Python（Flask） |
| フロントエンド | HTML / CSS / JavaScript（ローカルWebアプリ） |
| Markdownプレビュー | marked.js（CDN経由） |
| LLM API | **Google Gemini**（`google-generativeai` Pythonライブラリ経由） |
| データ形式 | Markdown（.md） / JSON |

---

## 3. ディレクトリ構成

```
memo-organizer/
│
├── app.py
├── config.json
├── requirements.txt
│
├── data/
│   ├── inbox.md
│   ├── tags.json
│   ├── category/ (動的に生成されるフォルダ例)
│   │   └── 簿記.md
│   ├── diary/ (動的に生成されるフォルダ例)
│   │   └── 2026-04-24.md
│   └── archive/
│       └── 2026-04-23_1500_raw.md
│
├── static/
│   ├── style.css
│   └── script.js
│
└── templates/
    └── index.html
```

---

## 4. コア機能とデータフロー

### フェーズ1：記録（書きなぐり）
1. ユーザーが中央ペインのエディタに自由にテキストを入力
2. 入力内容は数秒ごとに `data/inbox.md` へ自動保存
3. 入力ブロックごとにタイムスタンプをメタデータとして裏側で保持（LLMが文脈判断しやすくするため）

### フェーズ2：整理のトリガー
1. ユーザーがヘッダーの「整頓ボタン」を押下
2. 押下時点でのみ、`inbox.md` の内容を読み込みGemini APIへリクエスト開始

### フェーズ3：API連携と構造化処理（ワークフロー実行）

**整理ルール（ワークフロー）の仕組み**
ユーザーは複数のルール（例：「カテゴリ分類」「日報生成」）を定義できる。各ルールは「保存先フォルダ名」と「自由なプロンプト」を持つ。

**Gemini APIへのリクエスト**
1. 全てのアクティブなルールのプロンプトに対して、`inbox.md` の内容を送信する（複数回または並列処理）。
2. パースエラーを防ぐため、システム側で必ず以下のJSON出力制約をプロンプト末尾に強制付与する。

**システム固定の期待出力フォーマット**
```json
[
  {"filename": "保存先のファイル名.md", "content": "ファイルに書き込む内容"}
]
```

### フェーズ4：非破壊的再配置とアーカイブ

**最優先事項：データの非破壊性**

1. 各APIから返却されたJSONを解析
2. ルールで定義されたフォルダ配下の指定ファイル（例：`data/category/簿記.md` や `data/diary/2026-04-24.md`）の**末尾に追記**
3. すべてのルールの処理完了後、`inbox.md` の元テキストを `data/archive/YYYY-MM-DD_HHMM_raw.md` として**バックアップ移動**
4. `inbox.md` の中身をクリア
5. GUIの左ペインのファイルツリー表示（動的フォルダ）と、エディタ表示を更新

---

## 5. 非機能要件

### 5.1 データ保全性（最重要）
- `inbox.md` への入力は常にオートセーブされ、アプリの強制終了でもデータは失われない
- LLM処理後の元テキストは必ず `archive/` にバックアップしてから `inbox.md` をクリアする
- `category/` への追記は**既存内容を破壊しない末尾追記のみ**

### 5.2 セキュリティ
- APIキーはローカルの `config.json` に保存（暗号化は必須ではないが、Git管理外とする）
- `config.json` は `.gitignore` に追加することを推奨

### 5.3 エラーハンドリング
- Gemini API呼び出し失敗時：`inbox.md` は変更せず、UIにエラーメッセージを表示
- JSON解析失敗時：`inbox.md` は変更せず、UIにエラーメッセージを表示
- 上記いずれの場合も、ユーザーがリトライ可能な状態を保つ

---

## 6. 初期ファイルのサンプル

### `tags.json`（初期値）
```json
["簿記", "基本情報", "雑記", "その他"]
```

### `config.json`（初期値）
```json
{
  "gemini_api_key": "",
  "model_name": "gemini-1.5-flash",
  "autosave_interval_ms": 3000
}
```

---

## 7. 開発を進めるにあたっての注意事項

1. **非破壊性を最優先**：いかなる理由があっても、ユーザーのメモを消失させる処理は入れないこと。`inbox.md` の内容を消すのは `archive/` へのバックアップ成功後のみ。
2. **Geminiのレスポンス不安定性への対処**：LLMの出力が指定JSONフォーマットから逸脱する可能性を想定し、パース失敗時のフォールバック処理を実装する。
3. **タグ厳守**：LLMに `tags.json` 以外のタグを勝手に生成させない。プロンプト設計で明示的に禁止する。
4. **ローカル完結**：外部送信はGemini APIへのみ。それ以外の通信は発生させない。
