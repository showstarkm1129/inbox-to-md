# STEP.md — プロジェクト進捗管理

> このファイルは、各ステップの実装と動作確認を段階的に進めるための**唯一の作業仕様書**です。
> 各ステップ完了時にユーザーが動作確認を行い、問題なければ次のステップへ進みます。
>
> **AIへの指示には、原則としてこのファイルのみを使用してください。**
> 計画書（`計画書.md`）は凍結された意思決定ログであり、実装判断には使用しません。

---

## AIへの依頼プロトコル

ユーザーは各ステップの開始時に、以下の形式でAIに依頼します。

```
---STEP.md---
（STEP.md の全文を貼る）
---

現在のステップ：Step X
タスク：上記 Step X の実装内容をすべて実装してください。
前提：[前のステップで発生した変更・注意点があれば書く]
```

---

## 全ステップ共通のルール

1. **非破壊性の厳守**：いかなる理由があっても、`inbox.md` の内容を `archive/` にバックアップする前にクリアしてはならない。`category/` 配下への書き込みは既存内容を破壊しない末尾追記のみとする。
2. **ステップの順序を勝手に変更しない**：Step 3（設定UI）は Step 7（API連携）より前に実装する。Step 7 の動作確認にAPIキーが必須であり、`config.json` への手書きによる代替は認めない。
3. **ステップを飛ばさない**：各ステップは前のステップの動作確認完了を前提とする。
4. **推測で実装しない**：仕様に曖昧な点があれば、実装前にユーザーに確認すること。

---

## ステップ一覧

| Step | タイトル | 状態 |
|------|---------|------|
| 1 | Flask 基盤 + ディレクトリ自動生成 | ✅ 動作確認完了 |
| 2 | 3ペインUIレイアウト | ✅ 動作確認完了 |
| 3 | 設定UI（APIキー入力 + config.json 保存） | ✅ 動作確認完了 |
| 4 | エディタ機能（inbox/ 配下の読み書き + オートセーブ） | ✅ 動作確認完了 |
| 5 | Markdownリアルタイムプレビュー | ✅ 動作確認完了 |
| 6 | ファイルツリー（category/ 一覧表示 + 閲覧） | ✅ 動作確認完了 |
| 7 | ワークフロー設定UI（ルールごとのフォルダ・プロンプト定義） | ✅ 動作確認完了 |
| 8 | Gemini API 連携（複数ルールの同時実行） | ✅ 動作確認完了 |
| 9 | 非破壊的再配置 + アーカイブ処理 | ✅ 動作確認完了 |
| 10 | エラーハンドリング + 仕上げ | ⬜ 未着手 |

---

## Step 1: Flask 基盤 + ディレクトリ自動生成

### 目的
アプリの骨格を作り、Flask サーバーが起動して `index.html` を表示できる状態にする。

### 実装内容
- [x] `requirements.txt` を作成
  ```
  Flask>=3.0
  google-generativeai>=0.8
  ```
- [x] `app.py` を作成
  - Flask アプリの初期化
  - 起動時に `data/`, `data/category/`, `data/archive/` ディレクトリを自動生成
  - 起動時に `data/inbox.md`, `data/tags.json` が存在しなければ初期ファイルを作成
  - `config.json` が存在しなければデフォルト値で作成
  - `/` ルートで `index.html` を返す
- [x] `templates/index.html` を最小構成で作成（"Hello, Memo Organizer!" 程度）
- [x] `.gitignore` に `config.json` を追加

### 初期ファイルのデフォルト値

**`tags.json` 初期値：**
```json
["簿記", "基本情報", "雑記", "その他"]
```

**`config.json` 初期値：**
```json
{
  "gemini_api_key": "",
  "model_name": "gemini-1.5-flash",
  "autosave_interval_ms": 3000
}
```

### 動作確認ポイント
1. `pip install -r requirements.txt` が成功する
2. `python app.py` でサーバーが起動する
3. ブラウザで `http://localhost:5000` にアクセスしてページが表示される
4. `data/` 配下のディレクトリ・ファイルが自動生成されている

---

## Step 2: 3ペインUIレイアウト

### 目的
ヘッダー + 左ペイン（ファイルツリー）+ 中央ペイン（エディタ）+ 右ペイン（プレビュー）の基本レイアウトを構築する。
このステップでは、複数の下書きファイル管理に向けたUI、拡張子（.md/.txt）の考慮、3つの表示モード切替、ファイルツリーの折りたたみ機能のガワを作成する。

### レイアウト構造

```text
┌─────────────────────────────────────────────────────────────┐
│ ⚙️設定 [📄新規作成▼(.md/.txt)] [📝|🌗|👁️]      [✨整頓ボタン] │
├────────────┬─────────────────────┬──────────────────────────┤
│ ◀ ファイル │  エディタ           │  プレビュー              │
│ ツリー     │  (inbox/xxx.md等)   │  (Markdown または text)  │
│ ▼ inbox/   │  書きなぐり         │                          │
│  ...       │  エリア             │                          │
│ ▶ category/│                     │                          │
└────────────┴─────────────────────┴──────────────────────────┘
```

### 実装内容（追加UI）
- [x] `static/style.css` を更新
  - 3ペイン構成のFlexboxレイアウト（モダンなダークモード、グラスモーフィズム）
  - ペインの表示モード（エディタのみ、分割、プレビューのみ）を切り替えるCSSクラス
  - 左ペイン全体の折りたたみスタイル
- [x] `templates/index.html` を更新
  - ヘッダー：設定ボタン、**新規作成（md/txt）ドロップダウン**、**3モード切替ボタングループ**、整頓ボタン
  - 左ペイン：ペイン折りたたみ用トグルボタン、**inbox/ と category/ を分けたアコーディオン階層**
- [x] `static/script.js` にUIロジックを追加
  - 3モード切替のイベント処理（CSSクラスの付け替え）
  - 左ペインの折りたたみイベント処理
  - ファイルツリー階層（アコーディオン）の開閉処理

### 動作確認ポイント
1. ブラウザで3ペイン構成が正しくレンダリングされる
2. ヘッダーにボタンの配置枠がある
3. 各ペインが視認でき、画面を適切に分割している

---

## Step 3: 設定UI（APIキー入力 + config.json 保存）

### 目的
ヘッダーからAPIキーを設定・変更できるUIを実装する。後続のStep 7（API連携）の動作確認に必要なため、この時点で実装する。

### 実装内容
- [x] Flask API エンドポイント追加
  - `GET /api/config` — 現在の設定を返す（APIキーはマスクして返す）
  - `POST /api/config` — 設定を `config.json` に保存
- [x] ヘッダーに設定ボタンを配置
  - クリックでモーダル or ドロワーを開く
  - APIキー入力欄・モデル名選択・保存ボタン
- [x] 設定モーダルのスタイリング

### 動作確認ポイント
1. 設定ボタンをクリックすると設定画面が表示される
2. APIキーを入力して保存すると `config.json` に反映される
3. ページリロード後も設定が保持されている
4. APIキーが画面上でマスクされている

---

## Step 4: エディタ機能（inbox/ 配下の読み書き + リネーム + オートセーブ）

### 目的
中央ペインでテキストを入力し、`inbox/` 配下の各ファイルに自動保存される仕組みを実装する。また、複数の下書きを管理し、名前を自由に変更できるようにする。

### 実装内容
- [x] Flask API エンドポイント追加
  - `GET /api/inbox` — 下書きファイル一覧を返す
  - `GET /api/inbox/<filename>` — 指定ファイルの内容を返す
  - `POST /api/inbox/<filename>` — 指定ファイルを上書き保存
  - `POST /api/inbox/new` — 新規下書きファイル（連番）の作成
- [x] **ファイル名の変更（リネーム）機能**
  - `POST /api/inbox/<filename>/rename` — ファイル名変更API
  - UI上でファイル名をクリックして変更できる導線
- [x] `script.js` にエディタロジックを実装
  - ファイル一覧の取得・表示・切り替え
  - オートセーブ機能（config.jsonの設定間隔に準拠）
- [x] `index.html` の中央ペインにテキストエリアとファイル名表示エリアを配置

### 動作確認ポイント
1. ページ読み込み時に `data/inbox/` のファイルがリスト表示され、最初の一つが読み込まれる
2. テキスト入力後、数秒待つと該当するファイルにオートセーブされる
3. 新規作成ボタンから新しいファイルが増え、切り替えて編集できる
4. ファイル名をクリックしてリネームでき、リスト上の名前も更新される

---

## Step 5: Markdownリアルタイムプレビュー

### 目的
中央ペインの入力内容を右ペインでMarkdownとしてリアルタイム表示する。

### 実装内容
- [x] `index.html` に `marked.js` をCDN経由で追加
- [x] `script.js` にプレビューロジックを追加
  - テキストエリアの `input` イベントで右ペインのHTMLを更新
  - `marked.js` でMarkdownをHTMLに変換
- [x] プレビュー領域のスタイリング（コードブロック・見出し等のCSS）

### 動作確認ポイント
1. テキストエリアに `# 見出し` と入力すると、右ペインに `<h1>` として表示される
2. リスト・コードブロック・リンクなど基本的なMarkdown記法がレンダリングされる
3. 入力に追従してリアルタイムにプレビューが更新される

---

## Step 6: ファイルツリー（category/ 一覧表示 + 閲覧）

### 目的
左ペインに `data/category/` 配下のファイル一覧を表示し、クリックで右ペインに内容を表示する。

### 実装内容
- [x] Flask API エンドポイント追加
  - `GET /api/files` — `data/category/` 配下の `.md` ファイル一覧をJSON配列で返す
  - `GET /api/files/<filename>` — 指定ファイルの内容を返す
- [x] `script.js` にファイルツリーロジックを実装
  - ページ読み込み時と整頓処理完了時にファイル一覧を取得・表示
  - ファイルクリック時に内容を取得し、右ペインに表示
  - 「inbox に戻る」ボタンでエディタプレビューモードに復帰
- [x] ファイルツリーのスタイリング

### 動作確認ポイント
1. `data/category/` にテスト用の `.md` ファイルを手動で作成すると、左ペインに表示される
2. ファイルをクリックすると右ペインにその内容がMarkdownレンダリングされる
3. 「inbox に戻る」操作で右ペインがエディタプレビューに戻る

---

## Step 7: ワークフロー設定UI（ルールごとのフォルダ・プロンプト定義）

### 目的
ユーザーが自由に「整理ルール（ワークフロー）」を作成・編集できるUIを追加し、ファイルツリーを動的フォルダ構成に対応させる。

### 実装内容
- [x] `config.json` に `workflows` 配列を保存する仕組みを追加
  - 例: `[{"id": "w1", "name": "カテゴリ整理", "folder": "category", "prompt": "..."}]`
- [x] 設定モーダル（または別画面）に「ワークフロー設定」UIを追加
  - 新規ルールの追加、既存ルールの編集・削除
  - 各ルールには「表示名」「保存先フォルダ名」「プロンプト」を入力可能にする
- [x] `script.js` のファイルツリー（左ペイン）を動的に更新
  - `config.json` に登録された `folder` ごとにアコーディオン階層を自動生成する
  - `GET /api/files` エンドポイントを改修し、全てのワークフローフォルダの中身を取得できるようにする

### 動作確認ポイント
1. 設定画面から新しい整理ルール（例：保存先 `diary`、プロンプト自由）を追加・保存できる。
2. 追加したルールが `config.json` に正しく保存されている。
3. リロードすると、左ペインのファイルツリーに `inbox/` に加えて、定義したフォルダ（`category/`, `diary/` など）が表示される。

---

## Step 8: Gemini API 連携（複数ルールの同時実行）

### 目的
「整頓ボタン」を押すと、設定されたすべてのワークフローが実行され、それぞれのプロンプトに基づいた分類結果（JSON）を取得する。

### 実装内容
- [x] `app.py` に整頓用のエンドポイント `POST /api/organize` を追加
- [x] エンドポイントの処理ロジック実装
  1. `inbox.md` を読み込む
  2. `config.json` の `workflows` をループ処理
  3. 各ルールのプロンプト末尾に**絶対的なJSON出力制約（後述）**をシステム側で結合して Gemini に送信
  4. すべてのルールの結果（JSON配列）を収集してレスポンスとして返す
- [x] ヘッダーの整頓ボタンに機能を接続（処理中はローディング表示）

### システム強制プロンプト（バグ防止のための絶対ルール）
ユーザーのプロンプトの末尾に、プログラム側で必ず以下のテキストを結合してAPIに送信します。
```text
---
【システム絶対ルール】
あなたは上記の指示に従ってテキストを処理しますが、出力形式は絶対に以下のJSON配列のみとしてください。
Markdownのコードブロック(```json)や前置き・説明は一切含めず、純粋なJSON文字列のみを返してください。
[
  {
    "filename": "保存先のファイル名（例: 簿記.md, 2026-04-24.md）",
    "content": "ファイルに書き込む内容"
  }
]
```

### 動作確認ポイント
1. 複数の整理ルールが存在する状態で「整頓」を押すと、各ルールごとにGemini APIが呼び出される。
2. ユーザーがどんな適当なプロンプトを書いても、システム強制プロンプトのおかげで、必ず `[{"filename":"...", "content":"..."}]` のJSONが返ってくる。
3. （この時点ではファイルへの書き込みは行わず、取得したJSONをコンソール等で確認できればOK）

---

## Step 9: 非破壊的再配置 + アーカイブ処理

### 目的
Gemini から取得した結果に基づき、テキストを各フォルダに追記し、元テキストをバックアップする。

### 実装内容（順序厳守）
この順序を守らない実装は**データ消失リスクがあるため絶対禁止**。

1. [ ] Geminiの出力JSONをもとに、対応するフォルダ（例：`data/diary/`）の指定ファイル（例：`2026-04-24.md`）の末尾に追記
   - 追記時にタイムスタンプ付きの区切り線を挿入
   - 存在しないフォルダ・ファイルは新規作成
2. [ ] 元の `inbox.md` を `data/archive/YYYY-MM-DD_HHMM_raw.md` としてバックアップ
3. [ ] **バックアップファイルの存在とサイズを確認した後のみ** `inbox.md` をクリア
4. [ ] 処理完了後にファイルツリーとエディタを更新

### 動作確認ポイント
1. 整頓ボタン押下後、ルールごとのフォルダ（`category/`, `diary/`等）にファイルが生成/更新される。
2. `archive/` に元のメモがバックアップされている。
3. `inbox.md` がクリアされ、エディタも空になる。
4. 途中でパースエラー等の異常が発生した場合、`inbox.md` のクリアは行われずデータが保護される。

---

## Step 10: エラーハンドリング + 仕上げ

### 目的
アプリ全体の堅牢性を高め、リリース可能な品質にする。

### 実装内容
- [ ] エラーハンドリングの強化
  - Gemini API 呼び出し失敗時、またはパース失敗時のリトライ・フォールバック
  - 一部のルールのAPI呼び出しだけ失敗した場合のフェイルセーフ処理
- [ ] UI の最終調整
  - トースト通知（保存成功、整頓完了、エラー等）
  - ローディングスピナー（複数ワークフロー実行中のプログレス表示など）
  - キーボードショートカット（Ctrl+S で手動保存など）
- [ ] README.md の作成（セットアップ手順、使い方、動的ワークフローの仕組み説明）

### 動作確認ポイント
1. APIキーが無効な場合や、通信エラー時にわかりやすいエラーメッセージが表示される。
2. 全機能を通して一連の操作（書く → 整頓 → 各フォルダの確認）がスムーズに完了する。

---

## 状態の凡例

| アイコン | 意味 |
|---------|------|
| ⬜ | 未着手 |
| 🔧 | 実装中 |
| ✅ | 動作確認完了 |
| ❌ | 問題あり（修正必要） |

