## プロジェクト概要
ローカル動作のMarkdownメモ整理アプリ。
Flask（Python）+ HTML/CSS/JS。Gemini APIでメモを自動分類する。

## ファイル構成と役割
- `STEP.md`：実装仕様書。**各ステップの依頼時に参照する。**
- `計画書.md`：意思決定ログ。実装判断には使用しない。

## 絶対に厳守するルール
1. `inbox.md` は `archive/` へのバックアップ成功確認後にのみクリアする
2. `category/` への書き込みは末尾追記のみ。既存内容の上書き・削除禁止
3. `config.json` はGit管理外。コード中にAPIキーをハードコードしない

## 実装スタイル
- 1ステップ1コミットを意識した小さい変更単位で実装する
- 仕様に曖昧な点があれば実装前に確認する。推測で進めない
- エラーハンドリングはStep 9で一括対応するため、それ以前のステップでは最低限でよい

## 作業開始時の確認事項
ユーザーから「Step Xを実装してください」と依頼されたら、
まずSTEP.mdの該当ステップの実装内容と動作確認ポイントを確認してから着手する。

##　自律的なエラーチェック
- コード生成後、`python -m py_compile app.py && echo "OK"`を実行し、エラーの有無を確認する。
    - エラーが発生した場合対処する。エラーが発生しなければ次のステップに進む。
- 作成したコードにおいてロジック的に発生する可能性のあるエラー、ミスを自律的に思考し、テスト方法を自律的に構築する。
    - 構築したテスト方法を実行し、エラーの有無を確認する。問題がないことを確認後、次のステップに進む。